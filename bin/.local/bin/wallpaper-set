#!/usr/bin/env bash
if [[ -z "$1" || ! -f "$1" ]]; then
  notify-send -u critical "Wallpaper" "File not found:\n$1"
  exit 1
fi

swaymsg output "*" bg "$1" fill
notify-send -t 2000 "Wallpaper" "$(basename "$1")"
