#!/bin/bash

DESC="update-$(date +%Y%m%d-%H%M)"

if ! launch-tui yay -Syu; then
    exit 1
fi

snapper -c root create -c number -d "$DESC"
snapper -c root cleanup number

sudo grub-mkconfig -o /boot/grub/grub.cfg
