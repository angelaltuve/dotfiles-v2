#!/bin/bash

if pgrep -x swayidle >/dev/null; then
  echo '{"text": "󱫖", "alt": "active", "class": "active", "tooltip": "Idle locking enabled — click to disable"}'
else
  echo '{"text": "", "alt": "inactive", "class": "hidden"}'
fi
